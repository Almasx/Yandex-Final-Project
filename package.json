{
  "name": "yandex-final-project",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "server": "yarn workspace server run start:watch",
    "client": "yarn workspace client run dev",
    "dev": "concurrently \"yarn server\" \"yarn client\""
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "loadjs": "^4.2.0",
    "react-yandex-maps": "^2.3.0",
    "swiper": "^8.4.5"
  }
}
